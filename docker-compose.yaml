services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: f1-sim-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./src:/app/src
      - ./data:/data
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app/src
