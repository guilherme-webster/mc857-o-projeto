services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: f1-sim-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1
