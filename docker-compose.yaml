services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: f1-sim-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./data:/app/data:ro
    environment:
      - PYTHONUNBUFFERED=1
